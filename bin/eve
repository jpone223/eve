#!/usr/bin/env node

require('commander')
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('add', 'add a template to local')
  .command('init', 'generate a new project from a template')
  .command('list', 'list all local templates')
  .parse(process.argv)
